{% set name = "templateflow" %}
{% set version = "25.1.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/templateflow-{{ version }}.tar.gz
  sha256: 98ca78cd99662d3cdf6019209fbfce7518d514d21c692686bca33c32eb71a17f

build:
  noarch: python
  entry_points:
    - templateflow = templateflow.cli:main
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - hatchling
    - hatch-vcs
    - nipreps-versions
    - pip
  run:
    - python >={{ python_min }}
    - pybids >=0.15.2
    - importlib-resources >=5.7
    - requests
    - tqdm
    - platformdirs >=4.0
    - acres >=0.5.0

test:
  imports:
    - templateflow
  commands:
    - pip check
    - templateflow --help
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/templateflow/python-client
  summary: TemplateFlow Python Client - TemplateFlow is the Zone of neuroimaging templates.
  license: Apache-2.0
  license_file: LICENSE

extra:
  recipe-maintainers:
    - synchon
